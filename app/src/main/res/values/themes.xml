<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Launcher window theme: transparent and wallpaper-backed so the live wallpaper shows
      through behind the Compose surface (build brief §3 "wallpaper showThrough"). Compose
      provides all real styling via SphereTheme; this is purely the window shell.
    -->
    <style name="Theme.SphereLauncher" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>
